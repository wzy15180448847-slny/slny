<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.houserental.mapper.AgentQualificationMapper">

    <!-- 根据用户ID查询资质信息 -->
    <select id="selectByUserId" resultType="com.houserental.entity.AgentQualification">
        SELECT * FROM biz_agent_qualification WHERE user_id = #{userId} AND is_deleted = 0
    </select>

    <!-- 根据审核状态查询资质列表 -->
    <select id="selectByAuditStatus" resultType="com.houserental.entity.AgentQualification">
        SELECT * FROM biz_agent_qualification WHERE status = #{auditStatus} AND is_deleted = 0
    </select>

    <!-- 根据状态统计数量 -->
    <select id="countByStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM biz_agent_qualification WHERE status = #{status} AND is_deleted = 0
    </select>

</mapper>