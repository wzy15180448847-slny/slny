<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.houserental.mapper.WalletTransactionLogMapper">

    <select id="selectByUserId" parameterType="java.lang.Long" resultType="com.houserental.entity.WalletTransactionLog">
        SELECT id, user_id, type as transaction_type, amount, target_id as order_no, description as remark, create_time
        FROM biz_wallet_transaction
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByOrderNo" parameterType="java.lang.String" resultType="com.houserental.entity.WalletTransactionLog">
        SELECT id, user_id, type as transaction_type, amount, target_id as order_no, description as remark, create_time
        FROM biz_wallet_transaction
        WHERE target_id = #{orderNo}
        ORDER BY create_time DESC
    </select>

    <insert id="insertTransaction" parameterType="com.houserental.entity.WalletTransactionLog">
        INSERT INTO biz_wallet_transaction (user_id, type, amount, target_id, description, create_time)
        VALUES (#{userId}, #{transactionType}, #{amount}, #{orderNo}, #{remark}, NOW())
    </insert>

</mapper>